# Stack

> Questions

1. Implement Thread Safe Stack ? [Make all method synchronized]
2. Reverse the string using stack ? [Status : COMPLETED]
3. Balanced Parenthesis using stack ? [Status : COMPLETED]
4. Queue using two stacks ? [Status : COMPLETED]
5. Sort Stack ? [Status : COMPLETED]
