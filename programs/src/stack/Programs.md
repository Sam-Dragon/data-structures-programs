# Stack

> Questions

1. Implement Thread Safe Stack ? [Make all method synchronized]
2. Reverse the string ? [Status : COMPLETED]
3. Balanced Parenthesis ? [Status : COMPLETED]
4. Queue using two stacks ? [Status : COMPLETED]
5. Sort Stack ? [Status : COMPLETED]
6. Min Stack ? [Status: **PENDING**]
7. Next Greater Element ? [Status: **PENDING**] [Key: Reverse Loop + Stack]